const nextConfig = {
    /* config options here */
    reactStrictMode: false,
    // webpack: (config) => {
    //     config.module.rules.push({
    //         test: /\.(glsl|vs|fs|vert|frag)$/, // GLSL file extensions
    //         use: "raw-loader",
    //     });
    //     return config;
    // },
};

export default nextConfig;
