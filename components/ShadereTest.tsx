{
    /* <mesh>
                    <sphereGeometry args={[1, 32, 16]}></sphereGeometry>
                    <shaderMaterial
                        vertexShader={particlesVertexShader}
                        fragmentShader={particlesFragmentShader}
                        uniforms={{
                            uSize: new THREE.Uniform(0.4),
                            uResolution: new THREE.Uniform(
                                new THREE.Vector2(
                                    sizes.width ?? 0 * (sizes.pixelRatio ?? 0),
                                    sizes.height ?? 0 * (sizes.pixelRatio ?? 0)
                                )
                            ),
                        }}></shaderMaterial>
                </mesh> */
}
